export type ThemeColorSet = {
    name: string;
    colors: {
        primary: string;
        secondary: string;
        primaryText: string;
        secondaryText: string;
        primaryDisabled: string;
        secondaryDisabled: string;
    };
};
